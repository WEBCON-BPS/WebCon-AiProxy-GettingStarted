# Generated artifacts (certificates with private keys, secrets) - never package or commit these
.work/
*.pfx
*.pem
*.cer

# Compiled Bicep output (az bicep build) - templates live as .bicep, not committed JSON
**/bicep/*.json
